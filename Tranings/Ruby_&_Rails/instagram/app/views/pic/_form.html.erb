<%= form_with(model: pic, local: true) do |form| %>
  <% if pic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pic.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% pic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :pic_title %>
  </div>

  <div class="field">
    <%= form.label :desription %>
    <%= form.text_area :desription, id: :pic_desription %>
  </div>

  

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>